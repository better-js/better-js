# 配置

```bash
# 查看git版本
git -v

# 配置name和email，在提交代码时作为区分提交者身份的标识
git config --global user.name	"DancySue"
git config --global user.email "codebetter@163.com"

# 查看当前name和email
git config user.name
git config user.email
```

